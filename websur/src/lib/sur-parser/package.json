{
  "name": "sur-parser",
  "version": "1.0.0",
  "description": "Parser for SureScript notation",
  "main": "index.ts",
  "scripts": {
    "test": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "vitest": "^1.0.0",
    "@vitest/coverage-v8": "^1.0.0"
  }
}
